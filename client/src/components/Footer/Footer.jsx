import style from "./Footer.module.css"

const Footer = () => {
  return(
    <Footer className={style.footer}>
      <p>Pagina creada por el mas macho de todos los machos</p>
    </Footer>
  )
};

export default Footer;
